# ============================================================================
# Strict Docker Context (API/Worker image)
# Keep build context minimal: only files required by infra/standalone/Dockerfile.
# ============================================================================

# Ignore everything by default.
**

# Allow only runtime/build inputs.
!requirements.txt
!manage.py
!apps/
!apps/**
!voyant_project/
!voyant_project/**

# Optional metadata/docs useful inside image labels/debugging.
!README.md
!LICENSE
