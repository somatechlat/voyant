from django.apps import AppConfig


class DashboardBuilderConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.dashboard_builder"
    verbose_name = "Dashboard Builder"
